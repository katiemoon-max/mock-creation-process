# Question {{Q_NUMBER}} — {{TOPIC}}

{{QUESTION_STEM_COMMON_CONTEXT}}

{{IMAGE_PLACEHOLDER_IF_NEEDED}}

---

# Part a

## Problem

{{STEM_PART_A}}

## Solution

{{MODEL_ANSWER_A}}

**Mark Scheme and Guidance**

{{MSG_A}}

**End of Mark Scheme and Guidance**

**Examiner Tips and Tricks**

{{ETT_A}}

**End of Examiner Tips and Tricks**

---

# Part b

## Problem

{{STEM_PART_B}}

## Solution

{{MODEL_ANSWER_B}}

**Mark Scheme and Guidance**

{{MSG_B}}

**End of Mark Scheme and Guidance**

**Examiner Tips and Tricks**

{{ETT_B}}

**End of Examiner Tips and Tricks**

<!--
Template notes (remove before publishing):

Structure:
- The shared stem for the question (context + values + diagrams) lives above the first `# Part a`. It is NOT repeated in each part's Problem. Phase 5 embeds it into Part (a)'s Cobalt `problem` field; students reading later parts refer back to Part (a).
- No `**Model Answer**` wrappers. Write the solution content directly under `## Solution` — wrappers are not a valid Cobalt callout and would render as plain bold text.
- No `$m{[N mark(s)]}{align=right}` in the Problem section. Cobalt sets part marks via the per-part `marks` field during upload. Only use `{align=right}[N]` when labelling sub-parts like (i)/(ii) inside one part's problem.

Audits (enforced in /mock-3-draft):
- Every stem piece of info must be used by at least one part (stem-usage audit).
- No part should be answerable from info given in another part's stem (information-leakage audit).
- Every MCQ distractor must correspond to a named misconception in .project/misconception-bank.md.
- MCQ distractor explanations are paragraphs, not bullets.
- MCQ ET&T is optional (include only if it adds new insight beyond distractor commentary).
- Model answer word count must respect ceiling in .project/difficulty-targets.md.

Cobalt formatting:
- All equations in $$...$$ (double dollar). See cobalt-formatting skill.
- Mark indicators in Solution: $m{[1 mark]} inline on the step it awards.
- Final answer wrap: $f{...}. Working wrap: $w{...}. Commentary wrap: $c{...}.
- Callouts: Mark Scheme and Guidance, Examiner Tips and Tricks, Worked Example, Case Study, Blockquote.
- Valid Cobalt callouts do NOT include "Model Answer" — do not wrap solution content in it.
-->
